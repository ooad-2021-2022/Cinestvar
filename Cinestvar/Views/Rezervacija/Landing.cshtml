@model IEnumerable<Cinestvar.Models.Rezervacija>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdKorisnika)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Termin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdKorisnika)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Termin.IdTermina)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRezervacije">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRezervacije">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRezervacije">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
